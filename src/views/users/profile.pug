extends ../base
include ../mixins/video

block content
    div.profile__data
        if !user.avatarUrl
            span.avatarIcon 😀
        else
            img(src=user.avatarUrl, crossorigin="anonymous").avatar
        h3=user.name 
    div.video-grid
        each item in user.videos
            +video(item)
        else
            span.empty__message No videos found