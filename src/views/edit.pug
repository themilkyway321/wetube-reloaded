extends base.pug

block content 
    h4 Change Title of video
    form(method="POST")
        input(name="title", placeholder="Video Title", value=list.title, required, type="text")
        input(value="Save",type="submit")
